In this version, a basic framework for a logistic regression for reassigning transcripts have been established. However, the coefficients are still manually chosen. Since the way we test the reassignment is through another series of logistic regressions, it becomes hard to "learn" the coefficients unless we are willing to perform a grid search every time a user reads in their data or use some method to preset the coefficients. 
